# C语言
## 解题思路

采用**双指针（迭代）**法。
1. 定义 `prev` (前驱节点，初始为NULL) 和 `cur` (当前节点，初始为head)。
2. 遍历链表，每次先用 `next_temp` 保存 `cur->next`，防止断链。
3. 将 `cur->next` 指向 `prev`，完成反转。
4. 将 `prev` 和 `cur` 同时向后移动。
5. 遍历结束后，`prev` 即为新的头节点。

- **时间复杂度**: O(n)
- **空间复杂度**: O(1)
